алг
нач
  цел N = 20
  целтаб a[1:N]
  цел i, j, M
  нц для i от 1 до N
    ввод a[i]
  кц
  
  M := 1000
  нц для i от 1 до N
    если mod(a[i], 3) = 0 и mod(a[i], 8) > 4 и a[i] < M то
        M := a[i]
    все
  кц
  вывод M
кон
